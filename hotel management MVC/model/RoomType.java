package model;

// Enum representing different types of rooms
public enum RoomType {
    SINGLE,
    DOUBLE,
    SUITE,
    EXECUTIVE_SUITE,
    DELUXE_ROOM,
    FAMILY_ROOM,
    PENTHOUSE_SUITE,
    STANDARD_ROOM
}